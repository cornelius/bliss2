── v4: bliss show (unfiltered) ───────────────────────────────────────────────────

today
  1  Fix the deploy pipeline
  2  Review Alex's PR
  ──
  3  Write tests for auth

this-week
  4  Update documentation
  5  Refactor auth module


── v4: bliss show today (filtered — no header, no list name, no indent) ──────────

1  Fix the deploy pipeline
2  Review Alex's PR
──
3  Write tests for auth


── v4: bliss show (empty) ────────────────────────────────────────────────────────

All done. Nothing left to do.
